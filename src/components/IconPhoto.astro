---
interface Props {
    title: string;
}

const { title } = Astro.props;
---

<div class="flex flex-col items-center justify-center gap-5">
    <img src="/icon.jpg" alt="Logo" class="w-40 h-40 rounded-full" />
    <p class="w-72 text-center text-white text-2xl">{ title }</p>
</div>